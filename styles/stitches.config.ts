import { createStitches } from "@stitches/react";

export const {
    styled,
    css,
    globalCss,
    keyframes,
    getCssText,
    theme,
    createTheme,
    config
} = createStitches({

    theme:{
        colors:{
            primary:'#373738',
            secondary:'#757678',
            tertiary:'#DE6316',
            tertiaryHover:'#ff7d2b'
        }
    }
});